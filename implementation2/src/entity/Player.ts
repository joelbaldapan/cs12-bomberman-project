import { Model,
  Explosion,
  Bomb,
  Block,
  Player,
  Powerup,
  Entity,
  World,
  UpdateResult
} from "../model";

export const updatePlayer = (ent: Player, dt: number): [Player, UpdateResult] => {
  return Player.make({ /* implement */ })
}
