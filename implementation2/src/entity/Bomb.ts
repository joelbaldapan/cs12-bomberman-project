import { Model,
  Explosion,
  Bomb,
  Block,
  Player,
  Powerup,
  Entity,
  World
  // add others if needed
} from "../model";

export const updateBomb = (ent: Bomb, dt: number): Bomb => {
  return Bomb.make({ /* implement */ })
}
