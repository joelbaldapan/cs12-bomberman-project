import { Model,
  Explosion,
  Bomb,
  Block,
  Player,
  Powerup,
  Entity,
  World
  // add others if needed
} from "../model";

export const updateBlock = (ent: Block, dt: number): Block => {
  return Block.make({ /* implement */ })
}
