import { Model,
  Explosion,
  Bomb,
  Block,
  Player,
  Powerup,
  Entity,
  World,
  UpdateResult
} from "../model";

export const updateBlock = (ent: Block, dt: number): [Block, UpdateResult] => {
  return Block.make({ /* implement */ })
}
